# Core ASR
faster-whisper==1.1.1  # Pinned: 1.1.0+ required for large-v3-turbo (128 mel bins) support

# VAD
silero-vad>=5.1
torch>=2.0.0
torchaudio>=2.0.0

# Web Interface
gradio>=4.0.0

# YouTube Download
yt-dlp>=2024.1.1

# Audio Processing
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.10.0  # For audio resampling

# Spectrogram generation (Enhance-Only mode)
matplotlib>=3.7.0

# Speech Enhancement — DeepFilterNet3
deepfilternet  # DeepFilterNet3 noise suppression

# Speech Enhancement — DPDFNet (TFLite-based, lightweight)
# Prefer the standalone runtime to avoid pulling in all of TensorFlow.
# tflite-runtime wheels are available for Linux x86-64 / aarch64 on PyPI.
# If tflite-runtime is unavailable for your platform, install tensorflow instead.
tflite-runtime>=2.14.0  # DPDFNet TFLite inference (no GPU required)

# DPDFNet model download
huggingface_hub>=0.20.0  # already implied by transformers, listed for clarity

# Utilities
ffmpeg-python>=0.2.0
requests>=2.31.0

# Chinese Conversion (Simplified to Traditional)
opencc-python-reimplemented>=0.1.7

# Model Conversion
transformers
